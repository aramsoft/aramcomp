<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans" 
	xmlns:mvc="http://www.springframework.org/schema/mvc"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:context="http://www.springframework.org/schema/context"
    xsi:schemaLocation="http://www.springframework.org/schema/mvc 
						http://www.springframework.org/schema/mvc/spring-mvc.xsd
						http://www.springframework.org/schema/beans 
						http://www.springframework.org/schema/beans/spring-beans.xsd
        				http://www.springframework.org/schema/context 
						http://www.springframework.org/schema/context/spring-context.xsd">

	<context:property-placeholder location="classpath:/spring/conf/*.properties" system-properties-mode="OVERRIDE" />
 
	<!-- The controllers are autodetected POJOs labeled with the @Controller annotation. -->
	<context:component-scan base-package="aramframework">
		<context:include-filter type="annotation" expression="org.springframework.stereotype.Controller"/>
		<context:exclude-filter type="annotation" expression="org.springframework.stereotype.Service"/>
		<context:exclude-filter type="annotation" expression="org.springframework.stereotype.Repository"/>
	</context:component-scan>
 	
	<mvc:default-servlet-handler/>
    <mvc:annotation-driven />

	<!-- Handles HTTP GET requests for /resources/** by efficiently serving up static resources in the ${webappRoot}/resources directory -->
	<mvc:resources mapping="/resources/**" location="/resources/" />
    
	<mvc:interceptors>
		<mvc:interceptor> 
			<mvc:mapping path="/**" />
			<ref bean="tilesInterceptor" />
		</mvc:interceptor>
		<mvc:interceptor> 
			<mvc:mapping path="/**" />
			<ref bean="webLogInterceptor" />
		</mvc:interceptor>
	</mvc:interceptors>    

	<!-- Tiles 인터셉터 -->
	<bean id="tilesInterceptor" class="aramframework.cmm.config.interceptor.TilesInterceptor" > 
 		<property name="passURL">
			<set> 
				<value>/index.do</value>
				<value>/UnitMain.do</value>
				<value>/UnitLeft.do</value>
				<value>/UnitContent.do</value>
				<value>/PageLink.do</value>
				<value>/validator.do</value>
			</set>
		</property>
	</bean>

	<!-- 웹로그 인터셉터 -->
	<bean id="webLogInterceptor" class="aramframework.cmm.config.interceptor.WebLogInterceptor"> 
 		<property name="passURL">
			<set> 
				<value>/index.do</value>
				<value>/UnitMain.do</value>
				<value>/UnitLeft.do</value>
				<value>/UnitContent.do</value>
				<value>/PageLink.do</value>
				<value>/validator.do</value>
				<value>/uat/uia/[a-zA-Z]+\.do</value>
				<value>/WEB-INF/jsp/.*\.jsp.*</value>
			</set>
		</property>
	</bean>

	<!--
		- This bean processes annotated handler methods, applying Application-specific PropertyEditors
		- for request parameter binding. It overrides the default AnnotationMethodHandlerAdapter.
	-->
    <bean class="org.springframework.web.servlet.mvc.method.annotation.RequestMappingHandlerAdapter">
        <property name="webBindingInitializer">
            <bean class="aramframework.cmm.config.web.BindingInitializer"/>
        </property>
    </bean>
    
</beans>
