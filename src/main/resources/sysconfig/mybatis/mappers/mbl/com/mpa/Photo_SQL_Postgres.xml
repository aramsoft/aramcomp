<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper   PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="aramframework.mbl.com.mpa.dao.PhotoMapper">

	<select id="selectPhotoList" resultType="egovMap">
        /* 구현 Sql */
       	<![CDATA[
			SELECT SN
			     , MBER_ID
			     , PHOTO_SJ
			     , ATCH_FILE_ID
			     , CREAT_DT
			     , UPDT_DT		 
			  FROM MBLTN_PHOTO_ALBUM
			 WHERE 1=1
		]]>					
  	    <if test='searchVO.searchKeyword != null and searchVO.searchKeyword != ""'>
        <choose>
            <when test='searchVO.searchCondition == "MBER_ID"'>
             	<![CDATA[ AND MBER_ID LIKE '%'|| #{searchVO.searchKeyword} ||'%' ]]>
            </when>
            <when test='searchVO.searchCondition == "PHOTO_SJ"'>
             	<![CDATA[ AND PHOTO_SJ LIKE '%'|| #{searchVO.searchKeyword} ||'%' ]]>
            </when>
        </choose>
		</if>
		<![CDATA[
			 ORDER BY SN DESC			
		]]>				
        /* 구현 Sql */
        <![CDATA[
 			LIMIT  #{searchVO.recordPerPage} OFFSET #{searchVO.firstIndex}
        ]]>			
	</select>
	
	<select id="selectPhotoListCnt" resultType="int">
		<![CDATA[
			SELECT COUNT(*) totcnt
			  FROM MBLTN_PHOTO_ALBUM
			 WHERE 1=1
		]]>					
  	    <if test='searchVO.searchKeyword != null and searchVO.searchKeyword != ""'>
        <choose>
            <when test='searchVO.searchCondition == "MBER_ID"'>
             	<![CDATA[ AND MBER_ID LIKE '%'|| #{searchVO.searchKeyword} ||'%' ]]>
            </when>
            <when test='searchVO.searchCondition == "PHOTO_SJ"'>
             	<![CDATA[ AND PHOTO_SJ LIKE '%'|| #{searchVO.searchKeyword} ||'%' ]]>
            </when>
        </choose>
		</if>
	</select>
	
	<select id="selectPhoto" resultType="photoVO">
		<![CDATA[		
			SELECT SN
			     , MBER_ID
			     , PHOTO_SJ
			     , ATCH_FILE_ID
			     , CREAT_DT
			     , UPDT_DT	 			  			 			  
			  FROM MBLTN_PHOTO_ALBUM		
			 WHERE SN = #{sn}					
		]]>
	</select>
	
	<insert id="insertPhoto">
		<![CDATA[	
			INSERT INTO MBLTN_PHOTO_ALBUM (
					SN
				  , MBER_ID
				  , PHOTO_SJ
				  , ATCH_FILE_ID
				  , CREAT_DT
				  , UPDT_DT
			) VALUES ( 
					#{sn}
				  , #{mberId}
				  , #{photoSj}
				  , #{atchFileId}
				  , now()
				  , now() 
			)
		]]>
	</insert>

	<update id="updatePhoto">
		<![CDATA[
			UPDATE MBLTN_PHOTO_ALBUM 
			   SET SN			= #{sn}
				 , PHOTO_SJ		= #{photoSj}
				 , ATCH_FILE_ID = #{atchFileId}	
				 , UPDT_DT		= now()
 			 WHERE SN = #{sn}
		]]>
      	<if test='mberId != null and mberId != ""'>
 			   <![CDATA[ AND MBER_ID = #{mberId} ]]>
		</if>
	</update>
	
	<delete id="deletePhoto">
		<![CDATA[
			DELETE 
			  FROM MBLTN_PHOTO_ALBUM 
			 WHERE SN=#sn#
		]]>
      	<if test='mberId != null and mberId != ""'>
 			   <![CDATA[ AND MBER_ID = #{mberId} ]]>
		</if>
	</delete>
	
</mapper>
